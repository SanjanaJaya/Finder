specVersion: "v1beta"
serviceId: "finder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
